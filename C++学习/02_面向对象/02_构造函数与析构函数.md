# 02 - 构造函数与析构函数

## 🎯 学习目标
- 理解构造函数的作用和种类
- 掌握初始化列表的使用
- 理解析构函数的作用
- 掌握深浅拷贝的区别
- 理解拷贝构造函数和赋值运算符

---

## 📚 核心知识点

### 1. 构造函数

#### 默认构造函数
```cpp
class Student {
public:
    Student() {  // 默认构造函数
        name = "Unknown";
        age = 0;
    }
private:
    std::string name;
    int age;
};

Student stu;  // 调用默认构造函数
```

#### 带参数的构造函数
```cpp
class Student {
public:
    Student(const std::string& n, int a) : name(n), age(a) {}
private:
    std::string name;
    int age;
};

Student stu("Alice", 20);  // 直接初始化
Student stu2{"Bob", 21};   // 列表初始化（推荐）
```

#### 初始化列表
```cpp
class MyClass {
public:
    MyClass(int a, int b) 
        : member1(a), member2(b), const_member(100) {
        // 初始化列表在函数体之前执行
    }
private:
    int member1;
    int member2;
    const int const_member;   // const 成员必须用初始化列表
    int& ref_member;          // 引用成员必须用初始化列表
};
```

> **初始化列表优于函数体内赋值**：避免两次赋值，对 const 和引用成员是必须的。

### 2. 拷贝构造函数
```cpp
class String {
public:
    // 拷贝构造函数
    String(const String& other) 
        : data(new char[strlen(other.data) + 1]) {
        strcpy(data, other.data);
        std::cout << "Copy constructor" << std::endl;
    }
    
private:
    char* data;
};

// 调用时机
String s1("Hello");
String s2 = s1;        // 拷贝构造
String s3(s1);         // 拷贝构造
void foo(String s);    // 值传递参数
String bar();          // 返回值优化（RVO）
```

### 3. 赋值运算符
```cpp
class String {
public:
    // 拷贝赋值运算符
    String& operator=(const String& other) {
        // 1. 检查自赋值
        if (this == &other) return *this;
        
        // 2. 释放原有资源
        delete[] data;
        
        // 3. 分配新资源并拷贝
        data = new char[strlen(other.data) + 1];
        strcpy(data, other.data);
        
        return *this;
    }
};

// 使用
String s1("Hello");
String s2("World");
s2 = s1;  // 赋值运算符
```

### 4. 析构函数
```cpp
class String {
public:
    String(const char* str) {
        data = new char[strlen(str) + 1];
        strcpy(data, str);
    }
    
    ~String() {  // 析构函数
        delete[] data;  // 释放资源
        std::cout << "Destructor called" << std::endl;
    }
    
private:
    char* data;
};

// 析构时机
void test() {
    String s("Hello");  // 函数结束，s 被销毁
}  // 自动调用析构函数
```

### 5. 深浅拷贝
```cpp
// 浅拷贝：只拷贝指针值（默认行为）
class ShallowCopy {
    int* data;
    // 默认拷贝：data 指针被复制，两个对象指向同一块内存
};

// 深拷贝：拷贝指针指向的内容
class DeepCopy {
    int* data;
public:
    DeepCopy(const DeepCopy& other) 
        : data(new int(*other.data)) {}  // 分配新内存并拷贝值
};
```

### 6. 移动语义（C++11）
```cpp
class String {
public:
    // 移动构造函数
    String(String&& other) noexcept 
        : data(other.data) {
        other.data = nullptr;  // 将源对象置空
    }
    
    // 移动赋值运算符
    String& operator=(String&& other) noexcept {
        if (this == &other) return *this;
        delete[] data;
        data = other.data;
        other.data = nullptr;
        return *this;
    }
};

// 触发移动
String createString() { return String("Hello"); }
String s = createString();  // 移动构造（避免拷贝）
```

### 7. =default 和 =delete
```cpp
class MyClass {
public:
    MyClass() = default;                    // 显式使用编译器生成的默认构造
    MyClass(const MyClass&) = delete;       // 禁止拷贝
    MyClass& operator=(const MyClass&) = delete;  // 禁止赋值
};
```

---

## 💻 代码示例

```cpp
#include <iostream>
#include <cstring>

class String {
public:
    // 构造函数
    explicit String(const char* str = "") {
        data = new char[strlen(str) + 1];
        strcpy(data, str);
        std::cout << "Constructor: " << data << std::endl;
    }
    
    // 拷贝构造
    String(const String& other) : data(new char[strlen(other.data) + 1]) {
        strcpy(data, other.data);
        std::cout << "Copy constructor: " << data << std::endl;
    }
    
    // 移动构造
    String(String&& other) noexcept : data(other.data) {
        other.data = nullptr;
        std::cout << "Move constructor" << std::endl;
    }
    
    // 析构
    ~String() {
        std::cout << "Destructor: " << (data ? data : "null") << std::endl;
        delete[] data;
    }
    
    // 拷贝赋值
    String& operator=(const String& other) {
        if (this != &other) {
            delete[] data;
            data = new char[strlen(other.data) + 1];
            strcpy(data, other.data);
        }
        return *this;
    }
    
private:
    char* data;
};

int main() {
    String s1("Hello");
    String s2 = s1;       // 拷贝构造
    String s3("World");
    s3 = s1;              // 拷贝赋值
    return 0;
}
```

---

## ✏️ 练习任务

### 练习 1：实现简单数组类
实现一个 `Array` 类，支持：
- 构造函数（指定大小）
- 拷贝构造和赋值（深拷贝）
- 下标运算符 `[]`
- 析构函数（释放内存）

### 练习 2：禁止拷贝的类
设计一个资源管理类，禁止拷贝（使用 `=delete`），只允许移动。

### 练习 3：三/五法则
为一个包含动态内存的类，完整实现：
- 析构函数
- 拷贝构造函数
- 拷贝赋值运算符
- 移动构造函数（C++11）
- 移动赋值运算符（C++11）

---

## ❓ 常见问题

**Q: 构造函数和析构函数的调用顺序？**
A: 构造：基类 → 成员变量 → 自身；析构：自身 → 成员变量 → 基类（与构造相反）。

**Q: 为什么自赋值检查很重要？**
A: `s = s` 时，如果不检查，会先 `delete` 自己的资源，再试图读取已释放的内存。

**Q: 什么时候编译器会生成默认的拷贝构造/赋值？**
A: 如果没有自定义，编译器会生成。但一旦声明了移动构造/赋值，拷贝构造/赋值就被删除。
