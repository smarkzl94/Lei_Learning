# 02 - 内存管理

## 🎯 学习目标
- 理解 C++ 内存布局
- 掌握 new/delete 和 malloc/free 的区别
- 理解内存池和自定义分配器
- 掌握 Valgrind/AddressSanitizer 等工具检测内存问题

---

## 📚 核心知识点

### 1. C++ 内存区域
```
+------------------+
| 栈区 (Stack)     |  局部变量、函数参数，自动管理
+------------------+
| 堆区 (Heap)      |  new/malloc 分配，手动管理
+------------------+
| 全局/静态区       |  全局变量、静态变量
+------------------+
| 常量区           |  字符串字面量、const 全局变量
+------------------+
| 代码区           |  程序指令
+------------------+
```

### 2. new/delete vs malloc/free
```cpp
// malloc/free（C 风格）
int* p1 = (int*)malloc(sizeof(int) * 10);  // 分配 10 个 int
free(p1);

// new/delete（C++ 风格，推荐）
int* p2 = new int;          // 单个对象
delete p2;

int* p3 = new int[10];      // 数组
delete[] p3;                // 必须用 delete[]

// new 的优势：
// 1. 自动计算大小
// 2. 调用构造函数
// 3. 类型安全（无需强转）
```

### 3.  placement new
```cpp
// 在已分配的内存上构造对象
char buffer[sizeof(MyClass)];
MyClass* obj = new (buffer) MyClass();  // 在 buffer 上构造

// 手动调用析构函数（不释放内存）
obj->~MyClass();
```

### 4. 智能指针回顾
```cpp
// 现代 C++ 中应优先使用智能指针
std::unique_ptr<int> p1(new int(10));
auto p2 = std::make_shared<int>(20);  // 推荐

// 自定义删除器
std::unique_ptr<FILE, decltype(&fclose)> 
    file(fopen("test.txt", "r"), fclose);
```

### 5. 内存对齐
```cpp
// alignas 指定对齐要求
struct alignas(64) CacheLine {  // 64 字节对齐
    int data[16];
};

// alignof 查询对齐要求
std::cout << alignof(int) << std::endl;      // 通常是 4
std::cout << alignof(double) << std::endl;   // 通常是 8

// aligned_alloc（C++17）
void* p = aligned_alloc(64, 1024);  // 64 字节对齐，分配 1024 字节
free(p);
```

### 6. 内存检测工具
```bash
# Valgrind（Linux）
valgrind --leak-check=full ./program

# AddressSanitizer（GCC/Clang）
g++ -fsanitize=address -g program.cpp
./a.out

# 检测内容：内存泄漏、越界访问、使用未初始化内存、重复释放等
```

---

## 💻 代码示例

```cpp
#include <iostream>
#include <memory>

class Buffer {
public:
    explicit Buffer(size_t size) : size(size), data(new int[size]) {
        std::cout << "Buffer allocated: " << size << std::endl;
    }
    
    ~Buffer() {
        delete[] data;
        std::cout << "Buffer freed" << std::endl;
    }
    
    // 禁止拷贝
    Buffer(const Buffer&) = delete;
    Buffer& operator=(const Buffer&) = delete;
    
    // 允许移动
    Buffer(Buffer&& other) noexcept 
        : size(other.size), data(other.data) {
        other.data = nullptr;
        other.size = 0;
    }
    
    int& operator[](size_t index) { return data[index]; }
    
private:
    size_t size;
    int* data;
};

int main() {
    // RAII 内存管理
    {
        Buffer buf(100);
        buf[0] = 42;
    }  // 自动释放
    
    // 使用智能指针
    auto arr = std::make_unique<int[]>(1000);
    arr[0] = 1;
    
    return 0;
}
```

---

## ✏️ 练习任务

### 练习 1：内存泄漏检测
故意写一个内存泄漏的程序，用 AddressSanitizer 检测并修复。

### 练习 2：简单内存池
实现一个固定块大小的内存池：
- 预先分配一大块内存
- 提供 `allocate`/`deallocate` 接口
- 回收的内存块放入空闲链表复用

### 练习 3：RAII 封装
为一个 C 库资源（如 `FILE*` 或 `sqlite3*`）设计 RAII 封装类。

---

## ❓ 常见问题

**Q: `new` 失败会怎样？**
A: 默认抛出 `std::bad_alloc` 异常；可用 `new (std::nothrow)` 返回 nullptr。

**Q: 为什么 `delete[]` 要和 `new[]` 配对？**
A: `delete[]` 会调用每个元素的析构函数并记录数组大小；混用会导致未定义行为。

**Q: 什么是内存碎片？**
A: 频繁的分配和释放不同大小的内存，导致空闲内存被分割成小块，无法满足大内存分配需求。
