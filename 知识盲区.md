# 📝 知识盲区 & 错题本

> 记录学习过程中遇到的不理解、易混淆、报错的问题，形成闭环。

---

## 使用说明

1. **遇到问题时**：先记录到本文件和 Notion，不要立刻查答案
2. **解决问题后**：在"正解/笔记"栏写下自己的理解，而不是复制粘贴
3. **定期复习**：状态为"需复习"的条目每周过一遍

---

## 📋 记录格式

```markdown
### [状态] 问题标题

- **来源**：C++ / ROS / 通用
- **类型**：概念不懂 / 代码报错 / 易混淆 / 面试题
- **发现日期**：YYYY-MM-DD
- **关联知识点**：知识库中的对应序号

**问题描述**：
（当时为什么卡住了？错误信息是什么？）

**正解/笔记**：
（搞懂之后的总结，用自己的话写）
```

---

## ✅ 已理解

### 素数（质数）的判断方法

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-29
- **关联知识点**：03. 控制流程

**问题描述**：
忘记了素数的定义和判断方法。素数是大于1的自然数，只能被1和自身整除。1不是素数，2是唯一的偶素数。

**正解/笔记**：
核心思路：用 2 到 √n 的数去试除，能整除就不是素数。
- 优化1：只试到 √n（`i * i <= n`），因为因子成对出现
- 优化2：先排除偶数（除2外），只试除奇数（`i += 2`）
- 时间复杂度从 O(n) 降到 O(√n)

```cpp
bool isPrime(int n) {
    if (n <= 1) return false;   // 1 不是素数
    if (n == 2) return true;    // 2 是唯一的偶素数
    if (n % 2 == 0) return false;
    for (int i = 3; i * i <= n; i += 2) {
        if (n % i == 0) return false;
    }
    return true;
}
```

---

### constexpr 和 const 的区别是什么？为什么 constexpr 更苛刻？

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-29
- **关联知识点**：04. 变量与数据类型、19. 智能指针

**问题描述**：
const 只保证变量不被修改，但值可以在运行时确定；constexpr 要求值必须在编译期就能确定。之前误以为 const 就是编译期常量，导致在数组大小定义和模板参数使用时出错。

**正解/笔记**：
constexpr ⊂ const。所有 constexpr 都是 const，但反之不成立。const 管"只读"，constexpr 管"编译期可计算"。能用 constexpr 的地方性能更好（零运行时开销），且可用于数组大小、模板参数等编译期场景。

---

### if/for/while 单条语句省略花括号的危害

- **来源**：C++
- **类型**：代码报错 / 安全漏洞
- **发现日期**：2026-08-29
- **关联知识点**：03. 控制流程

**问题描述**：
C++ 允许 if/for/while 只有一条语句时省略花括号，但后续加代码时极容易误以为新加的语句也在控制块内。著名的苹果 goto fail 漏洞就是因为省略花括号导致的 SSL 证书验证被跳过。

**正解/笔记**：
永远写花括号，即使只有一行代码。缩进对编译器无意义，只看语法结构。

```cpp
// ❌ 危险
if (x > 0)
    doA();
    doB();  // 实际无条件执行！

// ✅ 正确
if (x > 0) {
    doA();
}
```

---

### C++17 if 带初始化的限制

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-29
- **关联知识点**：03. 控制流程

**问题描述**：
以为 C++17 的 `if (初始化; 条件)` 可以在初始化部分写多条语句，尝试写 `if (auto it = map.find(key); auto x = 1; it != end())` 编译报错。

**正解/笔记**：
`if` 带初始化中的"初始化"只能是**单条声明或表达式**，不是代码块。多条语句必须提前在外部初始化，或封装成函数。

```cpp
// ❌ 编译错误：初始化部分不能有多条语句
if (auto it = map.find(key); auto x = 1; it != map.end()) { }

// ✅ 正确：只有一条初始化语句
if (auto it = map.find(key); it != map.end()) { }
```

---

### 默认参数 vs 函数重载

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-30
- **关联知识点**：04. 函数

**问题描述**：
默认参数和函数重载都能实现"同一个函数名支持不同参数个数"，不清楚什么时候该用哪个，以及能不能互相替代。

**正解/笔记**：
- 只是参数个数不同 → 用**默认参数**（更简洁）
- 参数类型不同 → 必须用**函数重载**（默认参数做不到）
- 两者**不要混用**，容易出现二义性编译错误

```cpp
// ✅ 默认参数：参数个数不同，行为相同
void print(int x, int y = 10, int z = 20);

// ✅ 函数重载：参数类型不同
int add(int a, int b);
double add(double a, double b);

// ❌ 二义性：混用会导致编译错误
void func(int a, int b = 10);
void func(int a);
func(5);  // 编译器不知道调用哪个
```

---

### for 循环中 ++i vs i++

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-31
- **关联知识点**：02. 运算符与表达式、04. 函数

**问题描述**：
看到 `for (size_t i = 1; i < arr.size(); ++i)` 时，担心 `++i` 会漏掉 `i=1` 的初始值，也不确定 `++i` 和 `i++` 在 for 循环中是否有区别。

**正解/笔记**：
1. **不会漏掉 i=1**：`++i` 在循环体执行完之后才运行，第一次循环时 i 就是初始化的 1
2. **行为上**：`++i` 和 `i++` 在 for 循环增量表达式中**效果完全相同**
3. **性能上**：`++i` 更优（不需要保存旧值到临时变量），是 C++ 规范推荐写法
4. **养成习惯**：for 循环统一写 `++i`，迭代器等复杂类型时性能差异更明显

```cpp
// ✅ 推荐
for (int i = 0; i < n; ++i) { }

// 行为相同，但不推荐
for (int i = 0; i < n; i++) { }
```

---

### 指针常量 vs 常量指针

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-31
- **关联知识点**：05. 指针与引用

**问题描述**：
`int* const p` 和 `const int* p` 总是搞混，记不住哪个是指针不能变，哪个是内容不能变。

**正解/笔记**：
核心口诀：**const 在 * 右边修饰指针，在 * 左边修饰指向的内容**。

| 写法 | 读法 | 指针能否变 | 内容能否变 |
|------|------|-----------|-----------|
| `int* const p` | 指针常量 | ❌ 不能 | ✅ 能 |
| `const int* p` | 常量指针 | ✅ 能 | ❌ 不能 |
| `const int* const p` | 指向常量的常量指针 | ❌ 不能 | ❌ 不能 |

```cpp
int a = 10, b = 20;

int* const p1 = &a;   // 指针常量：p1 永远指向 a，但 *p1 可以改
*p1 = 100;            // ✅ OK
// p1 = &b;           // ❌ 错误！p1 不能指向别处

const int* p2 = &a;   // 常量指针：p2 可以指向别处，但 *p2 不能改
// *p2 = 100;         // ❌ 错误！不能修改指向的内容
p2 = &b;              // ✅ OK
```

---

### 数组作为函数参数退化为指针

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-31
- **关联知识点**：06. 数组与字符串

**问题描述**：
数组传入函数时为什么 `sizeof(arr)` 不再等于数组总大小？以为传数组就是传整个数组的副本。

**正解/笔记**：
C++ 中数组作为函数参数时会**退化为指针**，函数内 `arr` 实际上是一个指针，不再保有数组长度信息。因此 `sizeof(arr)` 得到的是指针大小（8字节），而不是数组总大小。必须在传参时额外传递数组长度。

```cpp
// ❌ 错误：arr 已退化为指针，sizeof(arr) == 8（指针大小）
void bad_print(int arr[]) {
    for (int i = 0; i < sizeof(arr) / sizeof(int); i++) { }  // 错误！
}

// ✅ 正确：同时传递数组大小
void print_array(const int* arr, size_t size);
void print_array(const int arr[], size_t size);
```

---

### 二维数组参数必须指定列数

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-31
- **关联知识点**：06. 数组与字符串

**问题描述**：
二维数组作为函数参数时，为什么不能只写 `int mat[][]`，而必须指定第二维的大小（如 `int mat[][4]`）？

**正解/笔记**：
二维数组在内存中是**连续存储**的（行优先）。编译器需要知道**列数**才能正确计算元素的内存偏移：`&mat[i][j] = base + i * 列数 + j`。如果不指定列数，编译器无法计算偏移地址。

```cpp
// ✅ 必须指定列数（第二维）
void print_matrix(int mat[][4], int rows);

// ❌ 编译错误：第二维大小不能省略
void bad_matrix(int mat[][], int rows);

// 替代方案：用指针数组或一维指针 + 手动计算偏移
void alt_matrix(int* mat, int rows, int cols);  // 访问：mat[i * cols + j]
```

---

### struct vs class 的使用约定

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-31
- **关联知识点**：07. 类与对象

**问题描述**：
`struct` 和 `class` 在语法上几乎一样，只是默认访问权限不同。不清楚实际项目中应该怎么选择，为什么有的地方用 struct，有的地方用 class。

**正解/笔记**：
这是 C++ 社区的**约定**（不是语法强制）：
- **`struct`**：默认 `public`，用于**纯数据结构**（POD，Plain Old Data）——只有数据成员，没有复杂行为封装
- **`class`**：默认 `private`，用于**有封装行为的抽象**——数据放 private，通过 public 接口访问，可以在 setter 中加入合法性检查

```cpp
// ✅ struct：纯数据聚合
struct Point {
    double x;  // 默认 public
    double y;
};

// ✅ class：有行为、有封装
class Rectangle {
public:
    double area() const { return w * h; }
    void setWidth(double v) { if (v > 0) w = v; }  // 合法性检查
private:
    double w, h;  // 默认 private
};
```

> 💡 记忆：数据裸奔用 struct，封装行为用 class。

---

### 继承中的构造与析构顺序

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-09-01
- **关联知识点**：09. 继承

**问题描述**：
创建 Derived 对象时，Base、Member、Derived 三者的构造和析构顺序容易搞混，不清楚为什么构造是基类→成员→自己，而析构是相反的。

**正解/笔记**：
核心原则（C++ 铁律）：

| 阶段 | 顺序规则 | 记忆口诀 |
|------|---------|---------|
| 构造 | 基类 → 成员 → 自己 | 从外到内（穿衣服） |
| 析构 | 自己 → 成员 → 基类 | 从内到外（脱衣服） |

原因：
1. 构造时，基类是 Derived 的"外壳"，必须先穿好内衣（Base），再穿外套（Member），最后系扣子（Derived）
2. 析构时，必须先拆自己（Derived），再拆成员（Member），最后拆基类（Base）
3. 如果先析构基类，Derived 的成员还依赖基类资源，会导致未定义行为

```cpp
// 构造输出：Base constructor → Member constructor → Derived constructor
// 析构输出：Derived destructor → Member destructor → Base destructor
```

---

### 继承中的名称隐藏（Name Hiding）

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-09-01
- **关联知识点**：09. 继承

**问题描述**：
派生类中定义了一个和基类同名的函数，发现基类的其他重载版本突然调不了了。以为继承后基类的函数都还在，或者以为只是"覆盖"了签名相同的那个。

**正解/笔记**：
C++ 的**名称隐藏**规则：派生类中只要出现**同名**函数（不管参数列表是否相同），就会隐藏基类中**所有同名**的重载版本。这不是重写（override），也不是重载（overload），而是作用域屏蔽。

```cpp
class Base {
public:
    void foo() { cout << "Base::foo" << endl; }
    void foo(int x) { cout << "Base::foo(int)" << endl; }
};

class Derived : public Base {
public:
    void foo() { cout << "Derived::foo" << endl; }
    // 只写了 void foo()，但 Base::foo() 和 Base::foo(int) 全被隐藏了！
};

Derived d;
d.foo();         // ✅ Derived::foo()
d.Base::foo();   // ✅ 显式调用基类版本
d.Base::foo(10); // ✅ 显式调用基类重载版本
// d.foo(10);    // ❌ 编译错误！Base::foo(int) 被隐藏，Derived 作用域里找不到
```

**解决方案**：
1. 如果确实需要基类的重载版本，用 `using` 声明引入：
   ```cpp
   class Derived : public Base {
   public:
       using Base::foo;  // 把 Base 的所有 foo 引入 Derived 作用域
       void foo() { }    // 现在 Derived 有三个 foo：Base::foo()、Base::foo(int)、Derived::foo()
   };
   ```
2. 如果只是想调用基类版本，显式加作用域 `d.Base::foo(10);`

> ⚠️ 注意：`override` 关键字只能用于虚函数重写，名称隐藏不是重写，不会报错提醒。

---

### 虚继承（Virtual Inheritance）与菱形继承

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-09-01
- **关联知识点**：09. 继承

**问题描述**：
B 和 C 都继承 A，D 又同时继承 B 和 C。D 对象里出现了两份 A 的成员，访问时产生二义性编译错误。

**正解/笔记**：
这是典型的**菱形继承**（Diamond Inheritance）问题。

```
        A (x)
       / \
      B   C
       \ /
        D
```

**解决方案**：在菱形继承的中间层使用**虚继承**。

```cpp
class B : virtual public A {};  // 虚继承
class C : virtual public A {};  // 虚继承
class D : public B, public C {}; // D 中只有一份 A
```

**核心变化**：
1. **内存**：D 对象中只有一份 A 的副本，不再浪费空间
2. **访问**：`d.x` 不再二义，直接访问唯一的那份
3. **构造顺序**：A → B → C → D，**A 由最终派生类 D 直接构造**，B 和 C 中的 A() 被跳过

**构造顺序铁律（含虚继承）**：
| 阶段 | 顺序 |
|------|------|
| 第一步 | 虚基类（按继承图从左到右、从上到下） |
| 第二步 | 非虚基类（按声明顺序） |
| 第三步 | 成员变量（按声明顺序） |
| 第四步 | 自己的构造函数体 |

**注意**：只有出现菱形继承时才用虚继承。普通继承不要滥用 `virtual`，会引入 vbptr 带来轻微性能开销。

---

### 为什么叫「虚」函数？和普通函数的本质区别

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-09-01
- **关联知识点**：09. 继承

**问题描述**：
不理解「虚函数」名字的来历，也不清楚它和普通函数在底层实现上到底有什么区别。

**正解/笔记**：
**"虚"的含义**：调用目标在运行时才确定，编译时不知道真正调用的是哪个函数，像是"虚拟地"指向一个待确定的地址。

**底层机制——虚函数表（vtable）**：
- 每个含虚函数的类，编译器自动生成一张函数地址表
- 每个对象多一个隐藏指针 `vptr`（8字节），指向所属类的 vtable
- 调用虚函数时：`*(obj->vptr[index])()` —— 通过查表间接跳转

```
Animal 的 vtable: [0] -> Animal::speak()
Dog 的 vtable:    [0] -> Dog::speak()

Animal* a = new Dog();
a->speak();  // 运行时通过 a->vptr[0] 找到 Dog::speak()
```

**普通函数 vs 虚函数对比**：

| 特性 | 普通函数 | 虚函数 |
|------|---------|--------|
| 绑定时机 | 编译期（静态绑定） | 运行期（动态绑定） |
| 调用方式 | 直接跳转固定地址 | 通过 vptr 查表间接调用 |
| 依赖类型 | 指针/引用的声明类型 | 实际指向的对象类型 |
| 多态 | ❌ 不支持 | ✅ 支持 |
| 内存开销 | 无 | 每个对象多一个 vptr |
| 性能 | 更快 | 稍慢（多一次查表） |

**一句话记忆**：普通函数是编译器直接拨号，虚函数是打给总机（vptr）让运行时转接。

---

### 成员函数后的 const 是什么意思？为什么位置在参数列表后面？

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-31
- **关联知识点**：07. 类与对象

**问题描述**：
看到 `double getArea() const { }` 时，不理解为什么 `const` 要放在参数列表后面，而不是像 `const int` 那样放前面。以为 `const` 是修饰返回值的。

**正解/笔记**：
参数列表后面的 `const` 修饰的不是返回值，而是**隐藏的 `this` 指针**。

```cpp
double getArea() const;  // 编译器实际理解为：double getArea(const Rectangle* this);
```

- `const` 放在后面 = `this` 指针指向的内容不可修改
- 表示**通过这个指针不能修改对象的成员变量**
- 常量成员函数里不能调用非 `const` 成员函数

| 写法 | 含义 | 能否修改成员 |
|------|------|------------|
| `void foo()` | `this` 是 `T*` | ✅ 能修改 |
| `void foo() const` | `this` 是 `const T*` | ❌ 不能修改 |

**记忆口诀**：`const` 在 `()` 后，锁的是 `this` 指向的内容。

---

### this 指针、静态成员、常量成员函数的作用分别是什么？

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-08-31
- **关联知识点**：07. 类与对象

**问题描述**：
`this` 指针、静态成员、常量成员函数这三个概念总是搞混，不清楚各自的作用和使用场景。

**正解/笔记**：

| 概念 | 作用 | 使用场景 |
|------|------|---------|
| **this 指针** | 指向当前对象的指针，区分成员变量和参数同名 | 构造函数参数和成员同名时：`this->name = name;` |
| **静态成员** | 属于类而不是对象，所有对象共享同一份 | 计数器、配置常量、单例模式 |
| **常量成员函数** | 承诺不修改对象状态，可被 const 对象调用 | getter 函数、只读操作 |

**this 指针**：
```cpp
class Student {
    std::string name;
public:
    Student(std::string name) {
        this->name = name;  // this->name 是成员变量，name 是参数
    }
};
```

**静态成员**：
```cpp
class Counter {
    static int count;  // 所有对象共享
public:
    Counter() { ++count; }
    static int getCount() { return count; }  // 不需要对象就能调用
};
int Counter::count = 0;  // 必须在类外初始化
```

**常量成员函数**：
```cpp
class Rectangle {
    double width, height;
public:
    double getArea() const {  // 承诺不修改任何成员
        return width * height;
    }
};

const Rectangle r(3, 4);
r.getArea();  // ✅ 只有 const 成员函数能被 const 对象调用
```

---

### override 和 final 的区别与使用场景

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-09-01
- **关联知识点**：10. 多态与虚函数

**问题描述**：
`override` 和 `final` 都是 C++11 引入的关键字，名字和位置相似，容易搞混各自的作用和使用场景。

**正解/笔记**：

| 关键字 | 作用 | 使用位置 | 核心含义 |
|--------|------|----------|----------|
| `override` | 确认重写基类虚函数 | 派生类成员函数 | "请编译器检查我确实在重写" |
| `final` | 禁止进一步重写/继承 | 派生类成员函数 / 类声明 | "到此为止，不许再动" |

**override —— 编译器帮我把关**：
```cpp
class Base {
public:
    virtual void foo(int x) const {}
};

class Derived : public Base {
public:
    void foo(int x) override {}  // ❌ 编译错误！const 不匹配，签名不一致
};
```
没有 `override` 时，上面的代码会静默变成"名称隐藏"而不是重写，多态失效且编译器不报错。加上 `override` 后签名不匹配会当场报错。

**final —— 切断继承链**：
```cpp
class Derived : public Base {
public:
    void foo() override final;  // 我重写了，但我的子类不许再重写
};

class FinalClass final {};     // 这个类禁止被继承
```

**一句话记忆**：
- `override` = 请编译器当保镖，确认我写的函数确实在重写基类虚函数
- `final` = 画一条红线，子类不许跨越

---

### 友元函数（Friend Function）

- **来源**：C++
- **类型**：概念不懂
- **发现日期**：2026-09-01
- **关联知识点**：11. 运算符重载

**问题描述**：
不理解友元函数是什么，为什么要用它。看到 `friend std::ostream& operator<<(...)` 时，不清楚 `friend` 关键字的意义。

**正解/笔记**：
友元函数是**被类特许访问其私有成员的外部函数**，打破了封装边界。

**为什么需要友元？经典场景：`operator<<` 重载**

成员函数形式的 `operator<<` 有问题：
```cpp
class Complex {
public:
    std::ostream& operator<<(std::ostream& os) {  // 成员函数
        os << real << " + " << imag << "i";
        return os;
    }
};

Complex c;
c << std::cout;  // ❌ 反直觉！实际是 c.operator<<(std::cout)
```

友元函数解决左操作数问题：
```cpp
class Complex {
    friend std::ostream& operator<<(std::ostream& os, const Complex& c);
};

std::ostream& operator<<(std::ostream& os, const Complex& c) {
    os << c.real << " + " << c.imag << "i";  // ✅ 可以访问 private
    return os;
}

std::cout << c << std::endl;  // ✅ 符合直觉
```

**使用频率**：
- **高频**：`operator<<` / `operator>>` 重载（几乎必须用友元）
- **中频**：数学类的二元运算符（需要支持 `5 + obj`）
- **低频**：单元测试、某些设计模式

> 💡 建议：学到 `<<` 重载就够了，不必刻意追求用友元。现代 C++ 趋势是减少友元使用。

**常见困惑点**：

1. **友元声明在类内，定义必须在类外？**
   不是必须，但推荐。友元函数**不是成员函数**，即使写在类内 `friend` 声明下面，它也是普通函数。通常声明在类内（告诉编译器"我允许它访问 private"），定义在类外（保持类定义简洁）。

2. **友元函数能访问 private，那它和成员函数有什么区别？**
   | | 成员函数 | 友元函数 |
   |--|---------|---------|
   | 归属 | 属于类 | 不属于类，是普通函数 |
   | `this` 指针 | ✅ 有 | ❌ 没有 |
   | 调用方式 | `obj.func()` | `func(obj, ...)` |
   | 访问 private | ✅ 可以 | ✅ 可以（被特许） |

3. **为什么 `operator<<` 不能用成员函数？**
   成员函数的第一个参数固定是 `this`（即类的对象），所以 `operator<<` 作为成员函数时，左操作数必须是 `Complex` 对象，导致调用变成 `c << cout`（反直觉）。友元函数不是成员函数，参数顺序自由，可以实现 `cout << c`。

4. **友元是"破坏封装"吗？**
   严格来说是，但它是**受控的破坏**——只有被类明确 `friend` 授权的函数才能访问。类比：你把家门钥匙给了信任的邻居，不是拆了围墙。

---

---

### vector 的 emplace_back vs push_back 区别

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-08-29
- **关联知识点**：12. 序列容器

**问题描述**：
都知道 push_back 是拷贝/移动元素进去，emplace_back 是原地构造。但具体什么时候用哪个、性能差异有多大，不太确定。

**正解/笔记**：
push_back：先构造临时对象，再拷贝/移动到 vector 中。emplace_back：直接在 vector 内存位置构造对象，省去一次拷贝/移动。对于复杂对象（如含 string、vector 成员的类），emplace_back 更高效；对于内置类型，两者几乎无差别。

---

---

### 智能指针参数传递方式的选择

- **来源**：C++
- **类型**：易混淆
- **发现日期**：2026-09-03
- **关联知识点**：12. 智能指针

**问题描述**：
不清楚 `shared_ptr` 按值传、按引用传、`unique_ptr` 转移所有权各自的使用场景和区别，不知道什么时候该用哪种方式。

**正解/笔记**：

```
调用者                    被调用函数
  │                          │
  ├── shared_ptr (值传递) ──→│ "我也持有一份，计数+1"
  │                          │
  ├── shared_ptr& (引用) ──→│ "我只看看，不增加计数"
  │                          │
  ├── unique_ptr (move) ──→│ "给你了，我以后不管了"
  │                          │
  ←── unique_ptr (返回) ───│ "创建好了，你拿去用"
```

| 传递方式 | 所有权变化 | 性能 | 安全级别 | 典型场景 |
|---------|-----------|------|---------|---------|
| `shared_ptr` (值) | 共享 | 慢（原子计数）| ⭐⭐⭐ | 异步、缓存、多线程 |
| `const shared_ptr&` | 无 | 快 | ⭐⭐ | 同步读取、性能敏感 |
| `unique_ptr` (值) | 转移 | 快 | ⭐⭐⭐ | 资源移交、工厂模式 |
| `T*` / `T&` | 无 | 最快 | ⭐ | 确定生命周期、底层 API |

**核心记忆点**：
- `shared_ptr` 按值传 = 我也需要确保对象活着（计数+1）
- `shared_ptr&` 按引用传 = 我只是临时用一下（不保证存活）
- `unique_ptr` 按值传 = 所有权彻底移交（调用者以后不能用）
- 默认优先用 `unique_ptr`，需要共享时才用 `shared_ptr`

---

### Lambda 闭包返回时的捕获陷阱

- **来源**：C++
- **类型**：代码报错 / 严重 bug
- **发现日期**：2026-09-03
- **关联知识点**：13. Lambda 表达式

**问题描述**：
函数返回 Lambda 时，如果捕获局部变量用了引用捕获 `[&]`，返回后局部变量已销毁，Lambda 里的引用变成悬空引用，调用时产生未定义行为。

**正解/笔记**：

**铁律：返回 Lambda 时，捕获局部变量必须用值捕获 `[x]`，绝不能用引用捕获 `[&x]`！**

```cpp
// ❌ 错误：引用捕获局部变量
auto make_bad(int factor) {
    return [&factor](int n) { return n * factor; };
    // factor 是局部变量，函数返回后销毁！
}

// ✅ 正确：值捕获，编译器会拷贝一份带走
auto make_good(int factor) {
    return [factor](int n) { return n * factor; };
}
```

**原因**：值捕获时，编译器在 Lambda 对象内部生成成员变量保存副本，Lambda 返回时副本跟着一起走；引用捕获只保存指针/引用，局部变量销毁后指针悬空。

---

### auto 会丢弃引用和顶层 const

- **来源**：C++
- **类型**：易混淆 / 代码隐患
- **发现日期**：2026-09-03
- **关联知识点**：14. auto 与类型推导

**问题描述**：
以为 `auto` 推导出的类型和原变量完全一致，实际上 `auto` 会**丢弃引用和顶层 const**，导致意外拷贝或权限丢失。

**正解/笔记**：

`auto` 的核心规则：**丢弃引用和顶层 const**。

```cpp
int x = 10;
int& ref = x;
const int c = 20;

auto a1 = ref;   // a1 是 int（引用被丢弃！）
auto a2 = c;     // a2 是 int（const 被丢弃！）

// 想要保留引用和 const，必须显式加：
auto& a3 = ref;       // a3 是 int&
const auto a4 = c;    // a4 是 const int
const auto& a5 = c;   // a5 是 const int&
```

**高频陷阱：范围 for 循环中的 auto**

```cpp
std::vector<std::string> vec = {"hello", "world"};

// ❌ 错误：s 是 string 的副本，每次循环都拷贝！
for (auto s : vec) { s += "!"; }  // 改的是副本，原 vec 不变

// ✅ 正确：s 是引用，不拷贝，直接修改原元素
for (auto& s : vec) { s += "!"; }

// ✅ 只读遍历：const auto& 避免拷贝
for (const auto& s : vec) { std::cout << s << "\n"; }
```

**记忆口诀：auto 是「裸类型」，想要引用/const 必须显式加 `&` / `const`**

---

### std::vector<bool> 的代理类陷阱

- **来源**：C++
- **类型**：代码报错 / 严重设计陷阱
- **发现日期**：2026-09-03
- **关联知识点**：14. auto 与类型推导

**问题描述**：
以为 `std::vector<bool>` 和普通 `vector<T>` 一样，`operator[]` 返回 `T&`。实际上 `vector<bool>` 做了位压缩优化，每个 bool 只占 1 bit，没有独立内存地址，所以 `[]` 返回的是一个**代理类对象**，不是 `bool&`。

**正解/笔记**：

```cpp
std::vector<bool> vec = {true, false};

auto elem = vec[0];       // ❌ elem 是 vector<bool>::reference（代理类），不是 bool&
auto& ref = vec[0];       // ❌ 编译错误！代理类是临时对象，不能绑左值引用

// ✅ 解决方案：不用 vector<bool>，用 vector<char> 或 deque<bool>
std::vector<char> flags = {1, 0};
auto& ref = flags[0];     // ✅ 完全正常
```

**核心原因**：
- `vector<bool>` 为了省空间，8 个 bool 压缩到 1 字节
- 每个 "元素" 没有独立地址，无法返回 `bool&`
- 只能返回代理类，代理类内部通过位操作读写

**业内共识**：`std::vector<bool>` 是 C++ 的设计失误，新项目避免使用。

---

### decltype 对表达式的推导规则

- **来源**：C++
- **类型**：概念不懂 / 易混淆
- **发现日期**：2026-09-03
- **关联知识点**：14. auto 与类型推导

**问题描述**：
不理解 `decltype(x = 20)` 为什么推导为 `int&`，以为应该和 `decltype(x)` 一样推导为 `int`。

**正解/笔记**：

`decltype` 对**变量名**和对**表达式**的规则不同：

| 写法 | 含义 | 推导结果 |
|------|------|---------|
| `decltype(x)` | 变量名本身 | `int`（原类型，不加引用）|
| `decltype((x))` | 带括号的表达式（左值） | `int&`（左值 → 加 `&`）|
| `decltype(x = 20)` | 赋值表达式（返回左值引用） | `int&`（左值 → 加 `&`）|
| `decltype(x + 20)` | 算术表达式（纯右值） | `int`（右值 → 不加）|

**核心铁律**：

> `decltype(表达式)`：左值加 `&`，右值不加。

```cpp
int x = 10;

decltype(x) a;         // int
decltype((x)) b = x;   // int&（括号让变量变成表达式，表达式是左值 → 加 &）

decltype(x = 20) c = x;  // int&（赋值表达式返回左值引用）
c = 999;                   // 改的是 x！

decltype(x + 20) d;    // int（x+20 是纯右值）
```

**记忆口诀：decltype 变量名看原类型，decltype 表达式看值类别（左值加&，右值不加）。**

---

### auto / decltype 类型推导速查表

- **来源**：C++
- **类型**：总结速查
- **发现日期**：2026-09-03
- **关联知识点**：14. auto 与类型推导

**正解/笔记**：

| 写法 | 推导结果 |
|------|---------|
| `auto x = expr` | 去掉引用和 cv |
| `auto& x = expr` | 保留引用，去掉 cv |
| `const auto& x = expr` | const 引用 |
| `auto&& x = expr` | 万能引用，完美转发 |
| `decltype(var)` | 变量声明的类型（含 cv 和引用） |
| `decltype(expr)` | 表达式的返回类型 |
| `decltype(auto) x = expr` | 按 decltype 规则推导 |

**关键区分**：
- `auto`：总是丢弃引用和 cv，想要保留必须显式加 `&` / `const`
- `decltype(变量名)`：原样保留声明类型
- `decltype(表达式)`：根据值类别推导（左值加 `&`，右值不加）

## ❓ 待解决

### CMakeLists.txt 中 add_executable 和 target_link_libraries 的顺序

- **来源**：ROS
- **类型**：代码报错
- **发现日期**：2026-08-29
- **关联知识点**：04. 创建功能包

**问题描述**：
编译时提示 undefined reference to `ros::init'，怀疑是链接顺序问题。add_executable 在 target_link_libraries 之前还是之后？

**正解/笔记**：
（待填写）

---

## Notion 同步

本文件与 Notion 数据库 `📝 知识盲区 & 错题本` 同步：
- Notion 地址：https://app.notion.com/p/63862c7c0a874581ae73a22bc1df16d6
- 建议：日常快速记录用本地文件，整理归档时同步到 Notion
