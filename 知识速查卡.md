# 📇 知识速查卡

> 遇到不熟悉、易忘、易混的知识点，提炼成卡片，回头过一遍就能快速复习。
> 
> 每张卡只保留三样东西：**知识点名 + 一句话总结 + 一段代码**。不记过程，只记结论。

---

## 🏷️ 标签说明

| 标签 | 含义 |
|---|---|
| `易忘` | 用完就忘，必须反复看 |
| `易混` | 和其他知识点容易搞混 |
| `重要` | 高频使用，必须掌握 |
| `面试` | 面试常考 |

---

## C++

### 素数判断
**一句话**：从 2 试除到 √n，能整除就不是素数；1 不是素数，2 是唯一的偶素数。

```cpp
bool isPrime(int n) {
    if (n <= 1) return false;
    if (n == 2) return true;
    if (n % 2 == 0) return false;
    for (int i = 3; i * i <= n; i += 2) {
        if (n % i == 0) return false;
    }
    return true;
}
```

`易忘` `面试`

---

### constexpr vs const
**一句话**：`constexpr ⊂ const`，constexpr 要求编译期就能算出值，const 只保证运行时不变。

```cpp
const int a = get_user_input();  // ✅ 运行时只读
constexpr int b = 42;            // ✅ 编译期常量
int arr[b];                      // ✅ 只有 constexpr 可以当数组大小
```

`易混` `重要`

---

### 永远写花括号
**一句话**：`if/for/while` 即使只有一行代码也必须写 `{}`，省略花括号是苹果 `goto fail` 漏洞的根源。

```cpp
// ❌ 危险：doB() 实际无条件执行
if (x > 0)
    doA();
    doB();

// ✅ 正确
if (x > 0) {
    doA();
}
```

`重要`

---

### C++17 if 初始化限制
**一句话**：`if (初始化; 条件)` 中的初始化只能是**单条声明或表达式**，不能是多条语句，也不是代码块。

```cpp
// ❌ 编译错误
if (auto it = map.find(key); auto x = 1; it != end()) { }

// ✅ 正确：只有一条初始化语句
if (auto it = map.find(key); it != map.end()) { }
```

`易混`

---

### 默认参数 vs 函数重载
**一句话**：参数个数不同用**默认参数**，参数类型不同用**函数重载**，两者混用会产生二义性。

```cpp
// ✅ 默认参数：个数不同，行为相同
void print(int x, int y = 10, int z = 20);

// ✅ 重载：类型不同
int add(int a, int b);
double add(double a, double b);

// ❌ 二义性：混用导致编译错误
void func(int a, int b = 10);
void func(int a);
func(5);  // 编译器不知道调用哪个
```

`易混` `面试`

---

### for 循环用 ++i
**一句话**：`for` 循环中 `++i` 和 `i++` 行为相同，但 `++i` 更高效（无临时变量开销），是 C++ 规范推荐写法。

```cpp
// ✅ 推荐：++i 无额外开销
for (int i = 0; i < n; ++i) { }

// 行为相同，但不推荐
for (int i = 0; i < n; i++) { }
```

`易混` `重要`

---

### 指针常量 vs 常量指针
**一句话**：`const` 在 `*` **右边**修饰指针（指针不能变），在 `*` **左边**修饰内容（内容不能变）。

```cpp
int* const p1 = &a;   // 指针常量：p1 不能指向别处，但 *p1 可以改
const int* p2 = &a;   // 常量指针：p2 可以指向别处，但 *p2 不能改
const int* const p3 = &a;  // 都不能变
```

`易混` `面试`

---

### 数组退化为指针
**一句话**：数组传入函数时退化为指针，`sizeof` 不再等于数组总大小，必须额外传递长度。

```cpp
// ❌ 错误：arr 已退化为指针
void bad(int arr[]) {
    int n = sizeof(arr) / sizeof(int);  // 错误！得到的是指针大小/4
}

// ✅ 正确：同时传递数组大小
void good(const int* arr, size_t size);
```

`易混` `重要`

---

### 二维数组参数必须指定列数
**一句话**：二维数组内存连续存储，编译器需要知道列数才能计算偏移，所以第二维大小不能省略。

```cpp
// ✅ 必须指定列数
void print_matrix(int mat[][4], int rows);

// ❌ 编译错误：第二维不能省略
void bad(int mat[][], int rows);

// 替代：展平为一维，手动计算偏移
void alt(int* mat, int rows, int cols);  // 访问：mat[i * cols + j]
```

`易混` `面试`

---

### struct vs class 使用约定
**一句话**：`struct` 默认 `public`，用于纯数据结构（POD）；`class` 默认 `private`，用于有封装行为的抽象。这是 C++ 社区的普遍约定，不是语法强制。

```cpp
// ✅ struct：纯数据，无行为封装
struct Point {
    double x;  // 默认 public
    double y;
};

// ✅ class：有行为、有封装
class Rectangle {
public:
    double area() const { return w * h; }
    void setWidth(double v) { if (v > 0) w = v; }  // 合法性检查
private:
    double w, h;  // 默认 private
};
```

`易混` `重要`

---

### 构造与析构顺序
**一句话**：构造由外到内（基类→成员→自己），析构由内到外（自己→成员→基类），像穿衣服和脱衣服。

```cpp
class Base {
public:
    Base()  { cout << "Base" << endl; }
    ~Base() { cout << "~Base" << endl; }
};

class Member {
public:
    Member()  { cout << "Member" << endl; }
    ~Member() { cout << "~Member" << endl; }
};

class Derived : public Base {
    Member m;
public:
    Derived()  { cout << "Derived" << endl; }   // 1.Base → 2.Member → 3.Derived
    ~Derived() { cout << "~Derived" << endl; }  // 1.~Derived → 2.~Member → 3.~Base
};
```

`易忘` `面试`

---

### 继承中的名称隐藏
**一句话**：派生类中只要出现同名函数，就会**隐藏基类所有重载版本**——不是覆盖，也不是重载，是彻底屏蔽。

```cpp
class Base {
public:
    void foo() { }
    void foo(int x) { }  // 重载版本
};

class Derived : public Base {
public:
    void foo() { }  // 只写这一个，Base::foo() 和 Base::foo(int) 全被隐藏！
};

Derived d;
d.foo();        // ✅ Derived::foo()
d.foo(10);      // ❌ 编译错误！Base::foo(int) 被隐藏了
d.Base::foo(10);// ✅ 显式指定作用域才能调用
```

修复方式：`using Base::foo;` 把基类重载版本引入当前作用域。

`易混` `面试`

---

### 虚继承（解决菱形继承）
**一句话**：菱形继承时，在中间层（B、C）用 `virtual public A` 继承，保证最底层（D）只有一份 A 的副本。

```cpp
class A { public: int x; };

class B : virtual public A {};  // ← 虚继承
class C : virtual public A {};  // ← 虚继承

class D : public B, public C {
    // D 中只有一份 A::x，不再二义
};

D d;
d.x = 10;        // ✅ 无二义，只有一份 x
d.B::x = 20;     // ✅ 同一地址
```

⚠️ 构造顺序：A → B → C → D，且 **A 由 D 直接构造**，跳过 B/C 中的 A()。

`易混` `面试`

---

### 虚函数 vs 普通函数
**一句话**：普通函数编译期直接调用（静态绑定），虚函数通过 vptr 查表运行时才确定调用谁（动态绑定）。

```cpp
class Base {
public:
    void normal() { }           // 普通函数
    virtual void virt() { }     // 虚函数
};

class Derived : public Base {
public:
    void normal() { }
    void virt() override { }
};

Base* b = new Derived();
b->normal();  // Base::normal()  ← 看指针类型
b->virt();    // Derived::virt()  ← 看实际对象
```

**内存代价**：每个对象多一个 `vptr`（8字节），指向类的虚函数表。

`重要` `面试`

---

## ROS

### （待补充）

---

## 使用方式

1. **遇到卡点时**：先自己总结成"一句话 + 一段代码"，再写到对应位置
2. **复习时**：遮住代码，只看标题和一句话，能想起代码就过，想不起来看一眼代码
3. **掌握后**：可以删掉或移到"已归档"，保持速查卡精简

---

## Notion 同步

本文件为主，Notion `📝 知识盲区 & 错题本` 数据库为辅：
- Notion 地址：https://app.notion.com/p/63862c7c0a874581ae73a22bc1df16d6
- 字段已精简为：知识点 / 一句话总结 / 代码速记 / 来源 / 标签 / 掌握状态 / 复习日期
